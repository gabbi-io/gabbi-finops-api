# gabbi-finops-poc (Flask) — Fase 1 (Custo)

PoC 100% fake, mas com funcionalidades idênticas ao produto final:
Interaction -> BillingAccumulator -> CostLedger

## Rodar local
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

pip install -r requirements.txt
python app.py
