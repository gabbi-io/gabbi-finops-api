# Core web
flask==3.0.2
flask-cors==4.0.0

# Server WSGI (produção)
gunicorn==21.2.0

# Azure OpenAI / OpenAI SDK
openai==1.12.0

# HTTP / Utils
requests==2.31.0
python-dotenv==1.0.1

# JSON / validação (se você valida payloads)
pydantic==2.6.1

# Logs estruturados (opcional, mas recomendado)
structlog==24.1.0
psycopg[binary]==3.2.9
